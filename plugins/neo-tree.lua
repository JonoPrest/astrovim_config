return {
    "nvim-neo-tree/neo-tree.nvim",
    config = {
        filesystem = {
            filtered_items = {
                visible = true,
                hide_dotfiles = false,
                hide_gitignored = true
            }
        }
    }
}
